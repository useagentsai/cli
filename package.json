{
  "name": "@useagents/cli",
  "version": "0.1.10",
  "description": "UseAgents registry CLI",
  "type": "module",
  "bin": { "useagents": "src/index.ts" },
  "files": ["src", "README.md", ".env.example"],
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build:bin": "mkdir -p dist && bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile dist/useagents"
  },
  "dependencies": { "@toon-format/toon": "^2.1.0", "commander": "^15.0.0", "ora": "^9.4.1" },
  "devDependencies": { "@types/bun": "latest", "typescript": "^5.7.3" }
}
